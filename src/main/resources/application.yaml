server:
  port: 8080

spring:
  datasource:
    username: root
    password: svist7890
    url: jdbc:mysql://localhost:3306/search_engine?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

# example values

indexing-settings:
  sites:
    - url: https://lenta.ru
      name: Лента.ру
    - url: https://siriusuniversity.ru
      name: SiriusUniversity.ru
    - url: https://www.playback.ru
      name: PlayBack.Ru